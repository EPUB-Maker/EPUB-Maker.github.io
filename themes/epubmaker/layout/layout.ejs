<%- partial('header') %>
<body>
  <div id="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
      <a class="navbar-brand" href="#">EPUBMaker</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <% 
            var currentLink = path.indexOf('zh-CN') !== -1 ? '/zh-CN/' : '/' 
            var langLink = path.indexOf('zh-CN') !== -1 ? '/' : '/zh-CN/' 
          %>
          <li class="nav-item">
            <a class="nav-link" href="<%= currentLink %>#home"><%= __('navigator.home') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= currentLink %>#download"><%= __('navigator.download') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= currentLink %>documentation"><%= __('navigator.documentation') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= currentLink %>#about"><%= __('navigator.about') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= langLink %>"><%= __('navigator.lang.0') %></a>
          </li>
        </ul>
      </div>
    </nav>
    <%- body %>
  </div>
</body>
</html>